# Document Standards — sigma.collections
- Standards: [q-or/DOC_STANDARDS.md](../q-or/DOC_STANDARDS.md)
- Registry:  [q-or/registry.anvl](../q-or/registry.anvl)
- This project's doc root: `sigma.collections/docs/`
- Cross-project link syntax: `../other-project/subdir/filename.md`
- To publish a doc for cross-project use: add entry to registry.anvl first
- ID prefix for new bugs: `BR-{YYMM}-sigma-collections-{seq:03d}`
- ID prefix for new features: `FR-{YYMM}-sigma-collections-{seq:03d}`
## Docs
- Register a new document: `python3 ../tools/doc-register.py --id <ID> --type <type> --owner sigma.collections --path "sigma.collections/subdir/file.md" --title "..." --status open --tags "tag1,tag2"`
- `--type` values: `bug-report`, `feature-req`, `ticket`, `use-case`, `backlog`
- Or edit [q-or/registry.anvl](../q-or/registry.anvl) directly; full conventions: [q-or/DOC_STANDARDS.md](../q-or/DOC_STANDARDS.md)
- Dependency state: check [q-or/repo.anvl](../q-or/repo.anvl) before starting feature work
- Registered FRs: [FR-2603-sigma-collections-001](feature-reqs/FR-2603-sigma-collections-001.md) — ✅ alloc_use seam + sigma.test opaque provider (v0.2.0)
- Registered FRs: [FR-2603-sigma-collections-002](feature-reqs/FR-2603-sigma-collections-002.md) — Map collection: string-keyed FNV-1a hash map with alloc_use support
## Ops
```
cbuild           # compile sources
cbuild lib       # compile + link shared lib
cbuild install   # install to /usr/local/packages/
cbuild clean     # remove build/ artifacts
./rtest          # run all tests
./rtest unit     # all tests in test/unit/
./rtest unit/<name>
```

### Dependencies
- Runtime: (none)
- cbuild/rtest are at `/usr/local/scripts/`; `./rtest` is the local wrapper
